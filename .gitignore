# Working data (state, confirmations, orders, AI answer memory, learned
# adapters, run history) changes while the program runs: kept out of git so
# a GitHub update never touches it.
app/data/

node_modules/
__pycache__/
*.pyc
*.tmp
.DS_Store

# Browser tests: dependencies and run artifacts.
prove-browser/node_modules/
prove-browser/test-results/
prove-browser/playwright-report/
